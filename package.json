{
  "name": "book-orbit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/dev-server.js",
    "check": "node --check public/main.js && node --check scripts/dev-server.js && node --check api/search.js && node --check api/image.js && node --check lib/book-api.js"
  },
  "engines": {
    "node": ">=20"
  }
}
